[project]
name = "power-bi-mcp"
version = "0.1.0"
description = "MCP Server for Power BI service management"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "httpx>=0.27.0",
]

[project.scripts]
power-bi-mcp = "server:main"